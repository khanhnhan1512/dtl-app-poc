Need to build a DTL app, that work across:
- Ubuntu
- Windows
- MacOS
- iOS
- Android
 
which is basically a webbrowser with certain modification (e.g. logo, mTLS for certain website, remote kill switch to wipe the app, custom home page, custom authentication mechanism)
 
This is only a proof-of-concept project, to see what it take to implement this